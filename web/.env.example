# Where the page looks for runs. The default reads what `urithiru publish` copied into
# public/runs, so the page needs no server and no cloud credentials. Point it at a public
# bucket prefix instead to read a run straight out of Cloud Storage.
PUBLIC_RUN_BASE=/runs
